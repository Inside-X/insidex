# Prisma / PostgreSQL connection string
# Keep credentials in environment variables, never hardcode secrets in source code.
#
# Parameters used below:
# - connection_limit: DB pool size (recommended for Prisma)
# - pool_timeout: max wait time (seconds) before failing when pool is exhausted
# - connect_timeout: timeout (seconds) for initial DB connection
# - sslmode: SSL behavior (prefer in dev, require/verify-full in prod)

DATABASE_URL="postgresql://DB_USER:DB_PASSWORD@DB_HOST:5432/DB_NAME?schema=public&connection_limit=10&pool_timeout=15&connect_timeout=10&sslmode=prefer"