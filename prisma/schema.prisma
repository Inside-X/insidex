// Prisma schema file for InsideX
// Database: PostgreSQL

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Add your models below, e.g.:
// model User {
//   id    String @id @default(cuid())
//   email String @unique
// }