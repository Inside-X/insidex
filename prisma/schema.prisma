// Prisma schema - base PostgreSQL configuration
// Models intentionally left empty for incremental migration from JSON storage.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// TODO: Add your models here, e.g. User, Product, Cart, Order, Lead, AnalyticsEvent.